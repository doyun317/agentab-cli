# agentab-cli v0.1.1 검증 기록

검증 일시: 2026-03-17 08:28 UTC  
검증 환경: Linux x86_64  
Go 버전: go1.25.0 linux/amd64  
릴리스 태그: `v0.1.1`

## 실행 항목

### 로컬 검증

- `go test ./...`
- `./scripts/release-snapshot.sh`

### 패키지 독립 실행 검증

새 release archive를 새 폴더에 풀어 아래 흐름을 검증했다.

- `doctor`
- `session start`
- `tab open`
- `tab text`
- `tab find`
- `tab click`
- `tab screenshot --save`
- `daemon stop`

검증 결과:

- 기존 글로벌 `127.0.0.1:9867` PinchTab runtime에 기대지 않고, 별도 로컬 포트에서 독립 PinchTab runtime이 기동됨
- 클릭 후 페이지 텍스트가 `ISOLATED-V2-OK`로 변경됨
- screenshot artifact 저장 확인

검증 artifact 예시:

- `/workspace/agentab-cli-release-smoke-isolated-v2/home/artifacts/isolated-v2/5B00CC556C16C1242D1EA70B6A19BE85/20260317T070722Z-screenshot.jpg`

### GitHub Release 검증

- 태그 push: `v0.1.1`
- GitHub Actions run: `23184931148`
- 결과: success
- Release URL: `https://github.com/doyun317/agentab-cli/releases/tag/v0.1.1`

### 공개 Release asset 재검증

GitHub Release에서 공개된 `agentab-cli_0.1.1_linux_x86_64.tar.gz`를 다시 다운로드해 별도 폴더에서 검증했다.

- `doctor`
- `session start`
- `tab open`
- `tab text`
- `tab find`
- `tab click`
- `tab snapshot --save`
- `tab snapshot --out`
- `tab screenshot --save`
- `tab pdf --save`
- `daemon status`
- `session stop`
- `daemon stop`

추가 확인 사항:

- `--output text` 성공 출력 확인
- artifact 저장 위치 확인
- 공개 asset 기준으로도 별도 로컬 PinchTab runtime 기동 확인

주의:

- `tab open` 직후 `tab list`가 새로 연 탭 대신 `about:blank`만 보여주는 사례가 한 번 있었다.
- 따라서 `tab list`는 이번 체크리스트에서 조건부 항목으로 남겨둔다.

## 메모

- `v0.1.0`은 release workflow가 실패했으므로 실제 사용 기준 버전은 `v0.1.1`이다.
