# agentab-cli v0.1.4 검증 기록

검증 일시: 2026-03-18 05:36 UTC
검증 환경: Linux x86_64  
Go 버전: go1.25.0 linux/amd64  
릴리스 태그: `v0.1.4`

## 실행 항목

### 로컬 검증

- `go test ./...`
- `./scripts/release-snapshot.sh`
- `./scripts/smoke-modes.sh`

### 릴리스 트리거 검증

- `main`에 `Prepare v0.1.4 release` 커밋 push
- 태그 push: `v0.1.4`
- GitHub Actions run: `23230629888`
- 결과: `success`
- Release URL: `https://github.com/doyun317/agentab-cli/releases/tag/v0.1.4`

### 로컬 smoke 확인

`scripts/smoke-modes.sh` 기준으로 아래를 확인했다.

- `headless`: `click_ok=True`, `type_ok=True`, `fill_ok=True`, `press_ok=True`, `scroll_ok=True`
- `headed`: `click_ok=True`, `type_ok=True`, `fill_ok=True`, `press_ok=True`, `scroll_ok=True`

### 공개 Release asset 재검증

GitHub Release에서 공개된 `agentab-cli_0.1.4_linux_x86_64.tar.gz`를 다시 다운로드해 별도 폴더에서 검증했다.

- 다운로드 경로: `/workspace/agentab-cli/tmp/release-v0.1.4-check/agentab-cli_0.1.4_linux_x86_64.tar.gz`
- 압축 해제 경로: `/workspace/agentab-cli/tmp/release-v0.1.4-check/extract`
- 실행 바이너리: `/workspace/agentab-cli/tmp/release-v0.1.4-check/agentab`
  메모: 압축 해제 직후 실행 시 `Text file busy`가 발생해, 같은 바이너리를 별도 경로로 복사해 검증에 사용했다.

결과:

- 신규 `AGENTAB_HOME` 기준 `doctor`가 정상 종료했다.
- `doctor --output text` 기준 `home`, `logs`, `artifacts`, `managed pinchtab bin`, `daemon log`, `pinchtab log`가 모두 출력됐다.
- `doctor` 기준 Chrome은 `source=env`로 탐지됐고, PinchTab은 고정 `9867`이 아닌 가용 loopback 포트(`http://127.0.0.1:35673`)에서 올라왔다.
- 공개 asset 바이너리를 `AGENTAB_BIN`으로 지정해 `scripts/smoke-modes.sh`를 다시 실행했고, `headless`와 `headed` 모두 `click`, `type`, `fill`, `press`, `scroll`을 통과했다.
- 액션 smoke 요약:
  - `headless`: `mode=headless`, `source=managed`, `click_ok=True`, `type_ok=True`, `fill_ok=True`, `press_ok=True`, `scroll_ok=True`
  - `headed`: `mode=headed`, `source=managed`, `click_ok=True`, `type_ok=True`, `fill_ok=True`, `press_ok=True`, `scroll_ok=True`

검증 artifact 예시:

- `/workspace/agentab-cli/tmp/release-v0.1.4-check/mode-smoke/headless/session-start.json`
- `/workspace/agentab-cli/tmp/release-v0.1.4-check/mode-smoke/headless/tab-text-after-scroll.json`
- `/workspace/agentab-cli/tmp/release-v0.1.4-check/mode-smoke/headed/session-start.json`
- `/workspace/agentab-cli/tmp/release-v0.1.4-check/mode-smoke/headed/tab-text-after-scroll.json`

## 메모

- `v0.1.4`는 운영성 개선, artifact 메타데이터 보강, 액션 smoke 확대를 묶은 patch release다.
