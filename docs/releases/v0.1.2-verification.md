# agentab-cli v0.1.2 검증 기록

검증 일시: 2026-03-17 08:53 UTC  
검증 환경: Linux x86_64  
Go 버전: go1.25.0 linux/amd64  
릴리스 태그: `v0.1.2`

## 실행 항목

### 로컬 검증

- `go test ./...`
- `./scripts/release-snapshot.sh`

### 릴리스 트리거 검증

- `main`에 `Prepare v0.1.2 release` 커밋 push
- 태그 push: `v0.1.2`
- GitHub Actions run: `23185918000`
- 결과: success
- Release URL: `https://github.com/doyun317/agentab-cli/releases/tag/v0.1.2`

### 공개 Release asset 재검증

GitHub Release에서 공개된 `agentab-cli_0.1.2_linux_x86_64.tar.gz`를 다시 다운로드해 별도 폴더에서 검증했다.

- `doctor`
- `session start`
- `tab open`
- `tab list`
- `tab find`
- `tab click`
- `tab text`
- `session stop`
- `daemon stop`

확인 결과:

- `doctor`가 `CHROME_BIN` override를 `chromeBin`으로 그대로 보여줌
- `doctor`가 `chromeBinFound=true`, `chromeBinSource=env`를 함께 보여줌
- `tab list`가 `currentTabId`를 포함하고, 첫 번째 탭 URL이 실제로 연 페이지 `http://127.0.0.1:18870/`로 보임
- 클릭 후 `tab text` 결과에 `V0.1.2-SMOKE-OK`가 포함됨
- 별도 `AGENTAB_HOME`과 별도 로컬 runtime 기준으로 독립 실행됨

검증 artifact 예시:

- `/workspace/agentab-cli/tmp/release-v0.1.2/doctor.json`
- `/workspace/agentab-cli/tmp/release-v0.1.2/tab-list.json`
- `/workspace/agentab-cli/tmp/release-v0.1.2/tab-text.json`

## 메모

- `v0.1.2`는 `v0.1.1` 이후 남아 있던 `tab list` stale 응답과 `doctor.chromeBin` 표시 문제를 공개 릴리스 기준으로 해결한 patch release다.
